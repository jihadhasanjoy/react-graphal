import { IRightLayoutProps } from "@/models/common";
import React from "react";
export default function CommonEditor({id, hideEditor}: IRightLayoutProps) {
  return (
    <div>User Editor</div>
  );
}